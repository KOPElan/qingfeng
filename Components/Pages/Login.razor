@page "/login"
@layout AuthLayout
@using QingFeng.Services
@inject IAuthenticationService AuthService
@inject NavigationManager Navigation
@rendermode InteractiveServer

<FluentStack Orientation="Orientation.Vertical" Style="min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 2rem;">
    <FluentCard Style="max-width: 450px; width: 100%; box-shadow: var(--elevation-shadow-dialog);">
        <FluentStack Orientation="Orientation.Vertical" Style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 2rem; text-align: center;">
            <FluentLabel Typo="Typography.H3" Style="color: white; margin: 0;">🔐 登录</FluentLabel>
            <FluentLabel Typo="Typography.Body" Style="color: rgba(255, 255, 255, 0.9); margin-top: 0.5rem;">欢迎回来，请登录您的账号</FluentLabel>
        </FluentStack>

        <FluentStack Orientation="Orientation.Vertical" Style="padding: 2rem;">
            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <FluentMessageBar Intent="MessageIntent.Error" Style="margin-bottom: 1rem;">
                    @errorMessage
                </FluentMessageBar>
            }

            <FluentTextField @bind-Value="username" 
                           Placeholder="请输入用户名" 
                           Label="用户名"
                           Style="width: 100%; margin-bottom: 1rem;"
                           @onkeydown="HandleKeyDown" />

            <FluentTextField @bind-Value="password" 
                           TextFieldType="TextFieldType.Password"
                           Placeholder="请输入密码" 
                           Label="密码"
                           Style="width: 100%; margin-bottom: 1.5rem;"
                           @onkeydown="HandleKeyDown" />

            <FluentButton Appearance="Appearance.Accent" 
                         OnClick="LoginAsync" 
                         Disabled="@isLoggingIn"
                         Style="width: 100%;">
                @if (isLoggingIn)
                {
                    <FluentProgressRing Style="width: 20px; height: 20px; margin-right: 0.5rem;" />
                    <span>登录中...</span>
                }
                else
                {
                    <span>🔑 登录</span>
                }
            </FluentButton>
        </FluentStack>
    </FluentCard>
</FluentStack>

@code {
    private string username = "";
    private string password = "";
    private string errorMessage = "";
    private bool isLoggingIn = false;

    [SupplyParameterFromQuery]
    public string? ReturnUrl { get; set; }

    private async Task LoginAsync()
    {
        errorMessage = "";

        if (string.IsNullOrWhiteSpace(username))
        {
            errorMessage = "请输入用户名";
            return;
        }

        if (string.IsNullOrWhiteSpace(password))
        {
            errorMessage = "请输入密码";
            return;
        }

        try
        {
            isLoggingIn = true;
            StateHasChanged();

            var user = await AuthService.LoginAsync(username, password);
            if (user == null)
            {
                errorMessage = "用户名或密码错误";
                return;
            }

            var redirectUrl = GetSafeRedirectUrl(ReturnUrl);
            Navigation.NavigateTo(redirectUrl, forceLoad: false);
        }
        catch (Exception ex)
        {
            errorMessage = $"登录失败：{ex.Message}";
        }
        finally
        {
            isLoggingIn = false;
            StateHasChanged();
        }
    }

    private string GetSafeRedirectUrl(string? returnUrl)
    {
        if (string.IsNullOrWhiteSpace(returnUrl))
            return "/";

        if (!returnUrl.StartsWith("/"))
            return "/";

        if (returnUrl.StartsWith("//"))
            return "/";

        if (returnUrl.Contains(":", StringComparison.OrdinalIgnoreCase))
            return "/";

        return returnUrl;
    }

    private async Task HandleKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter" && !isLoggingIn)
        {
            await LoginAsync();
        }
    }
}
